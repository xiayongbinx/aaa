server:
    port: 8090
    servlet:
        context-path: /peipei_test
spring:
    application:
        name: peipei_test #服务名称--调用的时候根据名称来调用该服务的方法
    profiles:
        active: dev
    datasource:
        url: jdbc:mysql://192.168.124.5:3306/peipei_test?serverTimezone=Asia/Shanghai
        username: peipei_test
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
#    jpa:
#        hibernate:
#            ddl-auto: update
#        show-sql: true

mybatis-plus:
    mapper-locations: classpath*:/META-INF/mapper/**/*Mapper.xml,classpath*:/META-INF/mapper/framework/dm/**/*Mapper.xml
    configuration:
        # 驼峰配置
        map-underscore-to-camel-case: true
        # 自动映射
        auto-mapping-behavior: full
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#showSql
logging:
    level:
        com.example.demo.mapper: debug
#management:
#    endpoints:
#        web:
#            exposure:
#                include: "*"
#    endpoint:
#        health:
#            show-details: ALWAYS

#eureka:
#  client:
#    serviceUrl: #注册中心的注册地址
#      defaultZone: http://127.0.0.1:8081/eureka/

